class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        """
        Do not return anything, modify nums1 in-place instead.
        """
        test = 0
        for i in range(m,m+n):
            # print("i", i)
            # print("nums2", nums2)
            # print("test", test)
            # print("nums2[test]", nums2[test])
            nums1[i] = nums2[test]
            test = test + 1
            # print(nums1)
        return nums1.sort()
